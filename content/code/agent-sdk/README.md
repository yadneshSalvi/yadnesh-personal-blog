# Claude Agent SDK in Production

Companion code for the series **[Claude Agent SDK in Production: Build an AI Data Analyst with FastAPI and Next.js](https://yadneshsalvi.com/series/claude-agent-sdk-fastapi-nextjs)** — an AI data analyst that reads your CSVs, writes and runs its own Python, and hands back charts and reports, built on the engine inside Claude Code.

Every folder below is the **complete, runnable project as it exists at the end of that part**. You can `cd` into any part and run it without assembling anything from earlier parts. All code was tested end to end against the live Claude Agent SDK before publishing. Each part folder also contains a short `demo.mp4` screen recording showing exactly what that part looks like when it runs.

| Part | Folder | What you build | Read along |
|---|---|---|---|
| 1 | [`part-01-first-agent`](part-01-first-agent) | A terminal agent that reads your CSVs and runs its own analysis | [Setup and Your First Agent](https://yadneshsalvi.com/blog/agent-sdk-1-first-agent) |
| 2 | [`part-02-fastapi-streaming`](part-02-fastapi-streaming) | The agent behind HTTP, streaming six event types over SSE | [The FastAPI Bridge and the Event Vocabulary](https://yadneshsalvi.com/blog/agent-sdk-2-fastapi-streaming) |
| 3 | [`part-03-agent-ui`](part-03-agent-ui) | A chat UI with live tool badges, markdown answers, and a Stop button | [The Agent UI](https://yadneshsalvi.com/blog/agent-sdk-3-agent-ui) |
| 4 | [`part-04-workspaces-artifacts`](part-04-workspaces-artifacts) | Per-conversation workspaces, file uploads, and an artifacts panel for charts and reports | [Workspaces and Artifacts](https://yadneshsalvi.com/blog/agent-sdk-4-workspaces-artifacts) |
| 5 | [`part-05-sessions`](part-05-sessions) | Session memory, a conversations sidebar, rename, and forkable analyses | [Sessions: The Analyst Remembers](https://yadneshsalvi.com/blog/agent-sdk-5-sessions) |
| 6 | [`part-06-custom-tools`](part-06-custom-tools) | A real SQLite database behind custom `@tool`s, served by an in-process MCP server, read-only by construction | [Custom Tools: Give the Analyst a Database](https://yadneshsalvi.com/blog/agent-sdk-6-custom-tools) |
| 7 | [`part-07-approvals`](part-07-approvals) | Human-in-the-loop approvals: risky tool calls pause on a Future until you click Approve or Deny | [Approvals: The Human in the Loop](https://yadneshsalvi.com/blog/agent-sdk-7-approvals) |
| 8 | [`part-08-hooks`](part-08-hooks) | Hooks: a PreToolUse tripwire that blocks `rm`, a JSONL audit log of every tool call, and injected desk facts | [Hooks: Guardrails and Audit Trails](https://yadneshsalvi.com/blog/agent-sdk-8-hooks) |
| 9 | [`part-09-durable-streams`](part-09-durable-streams) | Durable streams: a SQLite event log, replay-then-follow over `EventSource`, refresh-proof runs and approval cards, and a Stop button that actually stops | [Durable Streams: Survive the Refresh](https://yadneshsalvi.com/blog/agent-sdk-9-durable-streams) |
| 10 | [`part-10-interactive`](part-10-interactive) | Plan mode with a plan card and Implement button, AskUserQuestion as option chips over the Part 7 Future bridge, and an extended-thinking drawer | [Plan Mode, Questions, and Thinking You Can Read](https://yadneshsalvi.com/blog/agent-sdk-10-interactive) |
| 11 | [`part-11-subagents-skills`](part-11-subagents-skills) | A reviewer subagent with fresh eyes and a read-only toolbox rendered as nested badges, plus a beanline-report skill enforcing the house report format | [Subagents and Skills: The Analyst Grows a Team](https://yadneshsalvi.com/blog/agent-sdk-11-subagents-skills) |
| 12 | [`part-12-mcp-sandboxing`](part-12-mcp-sandboxing) | An external MCP server (`mcp-server-fetch`) for the open web, gated by an approval card; a prompt-injection page the analyst refuses; and a beta sandbox with OS-enforced filesystem and network walls (network escapes become cards) | [The Wider World: External MCP Servers and Sandboxing](https://yadneshsalvi.com/blog/agent-sdk-12-mcp-sandboxing) |
| 13 | [`part-13-structured-evals`](part-13-structured-evals) | Machine-readable summaries via `output_format` (the event vocabulary's final extension), a per-request `max_budget_usd` circuit breaker, and an eval suite that runs the real agent against SQL-verified cases with an LLM-as-judge | [Trust, but Verify: Structured Outputs, Budgets, and Evals](https://yadneshsalvi.com/blog/agent-sdk-13-structured-evals) |

*(The series is in progress — 14 parts across three acts. Folders appear here as parts publish.)*

## The sample data: Beanline

The series analyzes **Beanline**, a fictional specialty-coffee chain: six stores, twelve products, six months of daily sales (11,000+ rows). The dataset is generated by a deterministic script — same bytes on every run, on every machine — so the numbers in the series are reproducible on yours:

```bash
cd data
python3 generate_beanline.py
```

Each part folder ships with the generated CSVs already in place; you never need to run the generator unless you want to.

From Part 6 on, the same rows also live in a SQLite database. Build it with `python3 data/build_beanline_db.py` (or, inside a part, `uv run python data/build_beanline_db.py` from `backend/`); the file itself is gitignored because the builder is the source of truth.

## Running a part

Each part's own `README.md` has the exact steps. The backend uses [uv](https://docs.astral.sh/uv/):

```bash
cd part-0N-*/backend
uv sync
uv run python agent.py        # Part 1; later parts: uv run uvicorn app.main:app
```

**Frontend** (Parts 3+):

```bash
cd part-0N-*/frontend
npm install
cp .env.local.example .env.local
npm run dev
```

## Authentication

The Agent SDK uses your existing **Claude subscription login** (run `claude` once and log in) or an `ANTHROPIC_API_KEY`. [Part 1](https://yadneshsalvi.com/blog/agent-sdk-1-first-agent) covers both paths and how to cap spending. Keys, if you use one, go in `backend/.env` (gitignored). Never commit them.

## Versions

Pinned in each part's `pyproject.toml` / `uv.lock` — the exact versions the series was written and tested against: Python 3.13, `claude-agent-sdk` 0.2.110, FastAPI 0.136.x, Next.js 16.x, Node 22.
