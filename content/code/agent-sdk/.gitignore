# Python
.venv/
__pycache__/
*.pyc

# Secrets — never commit these
.env
.env.local

# Node / Next.js
node_modules/
.next/
next-env.d.ts

# Agent workspaces (per-conversation scratch dirs, Part 4+)
workspaces/

# Built artifacts (rebuild with data/build_beanline_db.py, Part 6+)
beanline.db

# The audit log the Part 8 hooks write (runtime data, not code)
audit.jsonl

# OS
.DS_Store
